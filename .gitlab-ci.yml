pages:
  stage: deploy
  image: alpine:latest
  script:
    - echo "Publishing Tranzor MR Pipeline demo to GitLab Pages"
  artifacts:
    paths:
      - public
  rules:
    - if: '$CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH'
